package com.edvora.utils

import android.Manifest

class Constants {
    companion object {
        var selectedColor: Int = 0 //applied color on drawing
        var strokeWidth: Float = 5f  //stroke width for drawing
    }
}